package models.daos.impl;

import models.daos.AbstractDao;
import models.impl.${name};

public class ${name}Dao extends AbstractDao<${name}> {
  private static final long serialVersionUID = 1L;

  public ${name}Dao() {
    super(${name}.class);
  }
}
